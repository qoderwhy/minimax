# 前端服务基地址（相对路径，构建后固化到产物）。
# 必须与后端 server.servlet.context-path=/admin-api 保持一致；接口路径中不要再重复出现 /admin-api，避免双前缀。
# dev：请求 /admin-api/** 由 Vite 代理转发到后端；prod：由 Nginx 反向代理。两端同一份代码路径，无需 CORS。
VITE_API_BASE_URL=/admin-api
VITE_APP_TITLE=后台管理系统

# 开发代理目标（仅服务端用，不进产物）：主后台 + 其它微服务按需加 PROXY_*
PROXY_ADMIN=http://localhost:8080
# PROXY_ANALYTICS=http://localhost:8081
